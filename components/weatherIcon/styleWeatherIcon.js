import { StyleSheet } from "react-native";

export const StyleWI = StyleSheet.create({
    container: {
        alignItems: "center",
        marginTop: 50

    },
    icon: {
        width: 180,
        height: 180,
        borderRadius: 90,
        backgroundColor: '#BFBFBF',
    }
})